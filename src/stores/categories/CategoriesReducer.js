// import * as Actions from './CategoriesAction';

const initialState = {
  income: [],
  expenses: [],
};

export default (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
};
